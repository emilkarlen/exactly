[setup]

def lines-transformer MY_TRANSFORMATION = TO_UPPER_CASE | replace '^HELLO, ' ''

[act]

hello-world -o output.txt

[assert]

contents output.txt  --transformed-by MY_TRANSFORMATION  every line : matches regex "^WORLD!$"
