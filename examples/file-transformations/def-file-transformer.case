#
# A "file-transformer" can be defined by the "def" instruction,
#
# with may improve readability, and also makes the transformer reusable.
#

[setup]


def file-transformer MY_1ST_TRANSFORMER = replace '[0-9]+' 'ID'

def file-transformer MY_2ND_TRANSFORMER = MY_1ST_TRANSFORMER | replace 'coffee' 'coffee with milk'


[act]


$ exit 0
