[conf]

status = FAIL

[setup]


file the-dir/checked-file.txt = <<EOF
actual contents
EOF


[assert]


dir-contents the-dir every file :
                         contents
                             -transformed-by to-upper-case
                                 ! any line : (
                                     matches 'CONTENTS' &&
                                     !
                                     line-num == 72
                                     )
