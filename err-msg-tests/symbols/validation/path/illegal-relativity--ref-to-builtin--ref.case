# Error: a path symbol has an illegal relativity (at point of use)
# via reference to a builtin symbol with illegal relativity

[setup]

def path WHAT_TO_INSTALL = @[EXACTLY_ACT]@

# Cause of error: "copy" only accept paths that are relative
# the home directory

copy @[WHAT_TO_INSTALL]@
