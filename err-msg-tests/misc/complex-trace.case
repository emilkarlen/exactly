[setup]


file the-dir/checked-file.txt = <<EOF
actual contents
EOF


[assert]


dir-contents the-dir every file :
                         contents
                             -transformed-by TO_UPPER_CASE
                                 ! any line :
                                     ( matches 'CONTENTS' &&
                                     ! line-num == 72 )
